# OS / editor cruft
.DS_Store
*.swp
*.log

# Plan de trabajo de la rama en curso (andamiaje, muere al integrar)
PLAN.md

# Yazi flavors — managed via package.toml (restore with `ya pkg install`)
yazi/.config/yazi/flavors/
